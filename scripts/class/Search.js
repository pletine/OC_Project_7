class Search {
    static mainInputSearch(textSearchValue) {
        let filteredArray = [];

        return filteredArray;
    }

    static ingredientTagFilter() {
        let filteredArray = [];

        return filteredArray;
    }

    static appareilsTagFilter() {
        let filteredArray = [];

        return filteredArray;
    }

    static ustensilsTagFilter() {
        let filteredArray = [];

        return filteredArray;
    }
}